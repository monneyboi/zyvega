[project]
name = "zyvega"
version = "0.1.0"
description = "PL103 Video Light Controller - Bluetooth LE control via CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.0",
    "dbus-next>=0.2.3",
]

[project.scripts]
videolight = "main:cli"
